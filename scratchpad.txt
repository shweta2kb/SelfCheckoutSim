


<label>Choose a browser from this list:
<input list="browsers" name="myBrowser" /></label>
<datalist id="browsers" th:each="product : ${products}">
<option th:value="${product.name}"></option>
</datalist>

<select name="select" th:each="product : ${products}">
  <option th:value="${product.name}"></option> 
</select>

<label>Current cart</label>
<table th:each="product : ${cart}">
	<tr>
		<td>
		<form method="post" name="remove"><input type="submit" th:value="${product.name}"></input></form>
		</td>
	</tr>
</table>

<label>Click on an item to add it to the cart: </label>
	<table th:each="product : ${products}">
		<tr>
			<td>
				<form method="post" action="newOrder/{cart}"><input type="submit" th:value="${product.name}"></input></form>
			</td>
		</tr>
	</table>

<form>
<div th:each="item : ${all}">
<label th:text="${item.name}"><input type="checkbox" th:field="*{product}" value="{item.name}"/></label>
</div>
</form>

<table th:each="item : ${cart}">
	<tr>
		<td th:text="${item.name}"></td>
	</tr>
</table> 


<form th:replace="base :: removeItem"></form>
<form th:replace="base :: checkout"></form>


<form method="post">
<label>Type in the name of the item you wish to add:
<input name="product" type="text" required="required"></input><button type="submit">Add item to cart</button>
</label>
</form>

//		if(i == null) {
//			String error = "Item was not found. Enter another item.";
//			model.addAttribute("error", error);
//			return "newOrder";
//		}

//		String product = request.getParameter("product");